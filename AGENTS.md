# AGENTS.md

本文件是本科研仓库中所有 AI 助手和自动化代理必须长期遵守的工作规则，适用于仓库根目录及全部子目录。若用户在当前任务中给出更具体的明确要求，以用户要求为准；但不得违反科研诚信、数据保留和事实准确性规则。

## 一、科研诚信与信息标注

1. 不允许编造论文结论、实验结果、引用，或用户尚未完成的科研进度。不确定的信息必须明确说明“不确定”或“尚未核实”。
2. 在研究笔记、汇报材料、实验记录和回答中，必须清楚区分以下四类内容：
   - **资料事实**：来自论文、官方文档或其他可核验来源的内容。
   - **个人理解**：用户或助手对资料的解释、概括与推导。
   - **研究假设**：尚待实验验证的猜想、预期或设计前提。
   - **实验结果**：已经实际运行实验并由保存的数据支持的观察或结论。
3. 涉及科研结论时，必须记录对应论文、官方资料或其他来源链接。无法定位来源时，不得把内容写成已经确认的事实。
4. 引用论文时应尽量记录标题、作者、会议或期刊、年份和稳定链接；引用二手资料时要明确标注其为二手来源。

## 二、实验与数据管理

5. 保留之前的实验版本，不要直接覆盖旧实验。新方案应使用新的版本号、日期目录、配置文件或 Git 分支进行保存。
6. 实验原始数据必须先完整保存，再进行清洗、汇总、绘图和分析。分析产物应能追溯到原始数据和对应配置。
7. 不允许在没有告诉用户的情况下修改实验假设、主要指标、基线、数据范围或成功标准。如确需调整，先说明原因及影响并获得确认。
8. 如果实验设计存在会显著影响结论的重要歧义，先询问用户，再继续实现。可以先完成不依赖该歧义的只读检查和准备工作。
9. 不得把尚未运行、运行失败、结果不完整或未经核对的实验写成“已验证”。

## 三、代码与文件修改

10. 修改重要代码后，必须用适合初学者理解的中文说明：改了什么、为什么这样改、如何验证，以及仍有哪些限制。
11. 如果一次任务需要修改很多文件，必须先向用户说明计划、影响范围和预计创建或修改的文件，再开始大规模修改。
12. 尽量保持实验可复现：记录环境、依赖版本、随机种子、模型与数据版本、运行命令和关键配置。
13. 不删除或覆盖用户已有的代码、数据、笔记和结果；如确有必要，必须提前说明并取得用户明确批准。

## 四、Git 工作流

14. 在执行 Git commit 之前，必须先向用户展示：
   - 创建了哪些文件；
   - 修改了哪些文件；
   - 最重要的变化是什么；
   - 建议的 commit message。
15. 除非用户明确批准，否则不要自动 commit，也不要自动 push。
16. 提交前应检查 `git status` 和相关 diff，避免把密钥、模型权重、数据集、大型 PDF 或临时实验输出纳入版本控制。

## 五、科研日志

17. 每日科研日志必须简洁、真实，并区分“已完成”“进行中”“遇到的问题”和“下一步”。
18. 不得把仅讨论、计划或尝试但尚未完成的事项写成已经完成；失败的尝试可以记录，但必须如实注明结果。
19. 日志中的实验结论应链接到对应实验目录、配置、原始数据或结果文件。

## 六、每日开工流程

20. 每次开始新的科研任务时，在修改任何文件、运行实验或开始新的分析之前，必须默认执行每日开工检查，不需要用户重复提醒。
21. 每日开工检查应读取并核对：
   - 最近一份 `daily_logs/YYYY-MM-DD.md`；如果尚无每日日志，应明确说明；
   - `ROADMAP.md`；
   - `RESEARCH_QUESTIONS.md`；
   - `PRO20X_QUEUE.md`；
   - 当前 Git 仓库状态。
22. 完成检查后，先用中文向用户说明：
   - 昨天实际完成了什么；
   - 当前研究进度到哪里；
   - 哪些问题仍然没有解决；
   - 今天最自然的下一步是什么；
   - 今天建议优先做哪些任务；
   - 哪些事情今天不建议做。
23. 在用户确认当天研究方向之前：
   - 不要修改文件；
   - 不要运行实验；
   - 不要 commit；
   - 不要擅自扩大任务范围。

## 七、每日结束流程

24. 每天科研任务结束时，根据当天真实完成的工作创建或更新 `daily_logs/YYYY-MM-DD.md`。
25. 每日日志至少记录：
   - 今天的目标；
   - 今天实际完成的内容；
   - 今天学到的东西；
   - 今天产生的实验或代码；
   - 尚未解决的问题；
   - 新产生的研究问题；
   - 下一步建议。
26. 日志只记录真正完成的内容。讨论过但没有执行的事情，不得写成已经完成。
27. 如果当天研究方向、研究问题或路线发生明显变化，再相应更新：
   - `ROADMAP.md`；
   - `RESEARCH_QUESTIONS.md`；
   - `README.md`。
28. 每日结束时如需 commit，仍必须先向用户汇报拟提交的文件、最重要的变化和建议的 commit message，并获得用户明确确认。
