# Research Questions

## 核心研究问题

**RQ0：能否利用预测的输出长度，帮助调度器做出更好的调度决策，从而改善大模型推理系统的性能？**

这里的“性能”可能涉及延迟、吞吐量和 GPU 利用率；具体主指标和成功标准尚未确定。

## 研究范围

- 当前重点是**预测输出长度**，不是预测输入长度。
- 当前关注预测信息如何影响 scheduling，而不是只优化预测器本身的数值误差。
- 在理解 vLLM、SGLang、S3 和 TIE 之前，暂不确定正式 baseline 和实验设计。

## 当前研究假设

### H1：相对排序可能是独立于绝对预测误差的重要因素

**个人理解：** 当多个请求的预测长度与真实长度数值不完全一致，但相对长短顺序保持不变时，基于长度排序的调度决策可能不发生变化。

**研究假设：** 对于基于输出长度的调度，绝对预测精度可能不是唯一重要因素；请求长度的相对排序质量也可能影响调度效果。

**当前依据：** 用户此前进行的简化 FCFS/SJF toy scheduling 思维实验和例子。

**证据状态：** 尚未经过代表性文献核验，也没有真实 GPU serving 实验验证。因此，该假设不能作为科研结论。

## 待回答的子问题

1. 输出长度通过执行时间、KV Cache 或其他资源路径影响哪些调度决策？
2. 输出长度预测器应向调度器提供点预测、相对排序、区间还是概率分布？
3. 什么类型和程度的预测误差会真正改变调度决策？
4. 预测指标与 waiting time、turnaround time、TTFT、TPOT、吞吐量和 GPU 利用率之间是什么关系？
5. vLLM 与 SGLang 的真实调度和内存管理机制，为长度预测提供了哪些可介入位置？
6. S3 与 TIE 分别如何使用输出长度信息，并处理预测错误或不确定性？
7. 简化 toy scheduler 中观察到的排序现象，在 continuous batching 和并行执行下是否仍然成立？

## 信息来源与状态说明

本页当前内容来自用户提供的项目历史背景和研究设想。除后续明确附有论文、官方资料或实验记录的内容外，均不应被解读为已验证结论。
